{
  "name": "aureval",
  "version": "1.0.0",
  "description": "Aureval — AI Response Evaluation Platform (Full-Stack)",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently --names \"API,UI\" --prefix-colors \"yellow,cyan\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently --names \"API,UI\" --prefix-colors \"yellow,cyan\" \"npm start --prefix backend\" \"npm run preview --prefix frontend\"",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
